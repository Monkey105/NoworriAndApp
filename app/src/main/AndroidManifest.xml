<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.noworri.noworri"
    android:targetSandboxVersion="1">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <application
        android:name=".Utils.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".TermsActivity"></activity>
        <activity
            android:name=".PaymentSelectActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".PaymentActivity"
            android:label="@string/title_activity_payment"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity android:name=".ContactSelectActivity" />
        <activity android:name=".ReceiverCountryActivity" />
        <activity
            android:name=".AddActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity android:name=".AddCardActivity" />
        <activity android:name=".RegisteredCard" />
        <activity
            android:name=".CodeVerifActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".RegisterPassActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".ForgotActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MobileVerifActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.Fullscreen" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>